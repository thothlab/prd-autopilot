---
name: prd
description: Use when the user dictates an initiative, feature or refactor to be specified and delivered end-to-end — PRD, spec deltas, tasks, code, reports — and expects the finished result rather than a collaboration on process. Also use when the user invokes /prd, asks for a PRD/ТЗ with task decomposition, asks to archive a finished PRD (/prd archive 02), or asks for a build in a named mode, depth or finish — «полный автомат», «режим интервью», «погриль меня», «ручной режим», «строго по брифу», «проработай глубоко», «вылижи до эталона».
argument-hint: "[full|semi|interview|manual] [strict|deep] [polish] описание инициативы или путь к файлу · archive NN"
---

# PRD

## Что это

`/prd` доводит инициативу от слов до готового результата **в одном диалоге**: разбирает запрос на требования, собирает контекст по репозиторию и журналам, пишет PRD и спек-дельты, режет их на задачи, исполняет каждую задачу отдельным субагентом и в конце сверяет построенное с изначальными словами пользователя. Пакет документов живёт в Obsidian-vault (или в самом репозитории, если vault не найден), код — в кодовом репозитории.

Скилл самодостаточен: все правила лежат в `phases/`, ставить рядом ничего не нужно.

Две идеи держат всю конструкцию.

**Порядок и есть продукт.** Код пишется в предпоследней фазе. Всё до неё существует, чтобы решить, *что* строить; всё после — чтобы доказать, что построено именно это.

**Запрос — это контракт, а не проект.** Отсюда два обязательства, и они намеренно тянут в разные стороны.

*Ничто не исчезает молча.* Слова пользователя становятся пронумерованным манифестом раньше всего остального, и каждая фаза сверяется с ним. Вайбкодинг ломает не плохой код — его ломает требование, которое перестало существовать где-то на третьей переформулировке.

*Запрос — не проект.* Это силуэт: happy path и ничего под ним — ни пустых состояний, ни отказов, ни обрывов, ни границ. Продумать их — законная работа, а не расползание рамок, и в ней большая часть ценности процесса. **Насколько далеко идти — ручка пользователя**, параметр [глубины](#три-ручки). Чего нельзя ни на одной настройке — глубины, **оторвавшейся** от запроса.

## Как читать этот скилл

Этот файл — оркестратор: режимы, порядок фаз, гейты. Правила каждой фазы лежат в `phases/` и **читаются в момент начала фазы, не раньше** — именно это держит рабочий контекст маленьким.

**По одному файлу за раз, и никогда вперёд.** Нарушение не ощущается нарушением: следующие файлы небольшие, маршрут понятен, открыть их «пока читаю» кажется аккуратностью. На деле это кладёт правила Фазы 5 в контекст, которым думает Фаза 1, и оставляет их там до конца прогона. Единица подгрузки — файл, а не раздел: чтение тянет всё целиком, поэтому то, что нужно одной фазе и не нужно другой, вынесено в отдельный файл.

**Дважды — только там, где так написано в таблице:** `7-instruments.md` в Фазе 4, `9-memory.md` в Фазах 5 и 8. Там это законно: между этими моментами прогон обычно успевает сжаться. Везде остальном перечитывание «детали подзабылись» покупает копию того, что и так лежит в контексте.

**После сжатия перечитывается состояние, а не фазы.** `state.js` (в нём `skillDir`, `prdRoot`, ревьюеры и задачи), `prd_XX_manifest.md`, `prd_XX_interfaces.md` и файл фазы, в которой ты сейчас, — эти четыре и больше ничего. Тянет открыть `5-subagents.md`, чтобы «вспомнить нить»; это тратит восемь тысяч токенов на перечитывание правил, которые ты уже исполняешь, а нити в них никогда не было.

| Фаза | Читать | Что производит |
|---|---|---|
| 0 Подготовка | `phases/0-modes.md`, `phases/0-preflight.md`, затем `0-instruments.md` и `0-memory.md` | режим объявлен, хранилище выбрано, `prdRoot` создан |
| 1 Требования | `phases/1-manifest.md` | `prd_XX_brief.md`, `prd_XX_manifest.md` |
| 2 Брифинг | `phases/2-briefing.md` + `references/research.md` | сбор контекста, `prd_XX_scratch.md`, ответы в манифесте |
| 3 PRD и спеки | `phases/3-prd.md` | `prd_XX_<slug>.md`, `specs/<domain>/spec.md` (дельты) |
| 4 План задач | `phases/4-plan.md` | `prd_XX_task_NN_<slug>.md`, `prd_XX_interfaces.md` засеян |
| 5 Разработка | `phases/5-subagents.md` | код, коммиты, отчёты, `interfaces.md` растёт |
| 6 Код-ревью | `phases/6-review.md` | ревью по трём осям на каждую задачу |
| 7 Приборы | `phases/7-instruments.md` — **в Фазе 4**, когда задачи нарезаны | `state.js`, `dashboard.html` (открыт пользователю) |
| 8 Приёмка | `phases/8-final.md` | слепая приёмка, итоговый отчёт |
| 9 Память | `phases/9-memory.md` — **в Фазе 5 и Фазе 8** | `CLAUDE.md` / `AGENTS.md`, запись в `Правки.md` |
| — | `phases/5-repair.md` — когда задача вернулась не с `DONE` | ремонт, ретраи, поправки спеки |
| — | `phases/10-archive.md` — только по команде `/prd archive NN` | дельты влиты в живые спеки, PRD в `Archive/` |
| — | `phases/rationalizations.md` — на провале гейта, когда ловишь себя на оправдании, и один раз перед отчётом | ничего; это чеклист |
| — | `phases/polish.md` — только с параметром `polish` | круги доводки |

## Слова, которые видит пользователь

Фазы в этом файле названы по-английски, и пользователь этих названий не видит никогда. В чате, на дашборде и в отчёте у каждого этапа **ровно одно русское слово**, и оно вот это. Два словаря на один процесс — так человек читает README и потом не находит на экране ничего из прочитанного.

| Фаза | `stages[].id` | Пользователю |
|---|---|---|
| 0 Preflight | `preflight` | Подготовка |
| 1 Manifest | `manifest` | Требования |
| 2 Briefing | `briefing` | Брифинг |
| 3 PRD | `spec` | PRD и спеки |
| 4 Plan | `plan` | План задач |
| 5 Build | `build` | Разработка |
| 6 Review | `review` | Код-ревью |
| 8 Final | `final` | Приёмка |

Три правила держат словарь:

- **«Прогон» — это весь заход целиком, а не один этап.** «Прогон идёт», «прогон прервался», «продолжи прогон» — про процесс. Поэтому пятый этап называется «Разработка».
- **Единица работы — «задача».** Не «таск», не «тикет», не «issue»: файлы в vault называются `prd_XX_task_NN_*`, и в отчётах, и в `План задач и отчёты.md` это всегда «задача». **В `state.js` поле при этом называется `tickets`** — так его читает шаблон дашборда, и переименовывать его не нужно; наружу оно выходит словом «задача».
- **То, что заказал пользователь, — «инициатива» или «запрос».** Не «задача»: одно слово на две разные вещи ломает и отчёт, и дашборд.

Фазы 7 и 9 не последовательные, и каждая разрезана надвое по линии, где её читают. Приборы поднимаются в Фазе 0 из `phases/0-instruments.md`, а `phases/7-instruments.md` открывается только когда нарезаны задачи. Память проекта поднимается в Фазе 0 из `phases/0-memory.md`, а `phases/9-memory.md` открывается, когда сборка что-то выяснила, и снова в Фазе 8.

## Три ручки

Всё, что напечатано после `/prd`, делится на четыре части: **режим** (`full`, `semi`, `interview`, `manual` — по умолчанию `semi`), **глубина** (`strict`, `deep` — по умолчанию обычная), **отделка** (`polish` — выключена) и **сам запрос** (всё остальное). Голые слова, без дефисов; нераспознанное — всегда запрос.

**Правила всех трёх — в `phases/0-modes.md`, читается в Фазе 0 вместе с `phases/0-preflight.md`**: триггеры на двух языках, вступительный блок, что разрешает каждая глубина и что происходит при переключении по ходу. Решаются они один раз, до Фазы 1, и дальше каждая фаза только *применяет* их.

Здесь остаётся следствие — таблица в разделе [Маршрут](#маршрут). Две вещи про ручки не двигаются никогда и повторены там, потому что это не калибровка: **ни один режим не снимает гейты манифеста и ни один не снимает гейты безопасности.**

## Субагенты — почему их можно звать

Харнесс требует явной просьбы пользователя, прежде чем оркестратор начнёт запускать субагентов. **Вызов `/prd` и есть эта просьба.** Скилл описан — во frontmatter, в README и во вступительном блоке Фазы 0 — как процесс, где каждая задача исполняется отдельным субагентом в чистом контексте; пользователь, набравший `/prd`, просит именно этот процесс. Отдельного слова-переключателя больше нет и не нужно.

**Если запуск субагентов в конкретном харнессе всё равно заблокирован**, режим деградирует, а не падает: фазы 0-4 и 8 работают как написано; в фазе 5 задачи исполняются последовательно в текущем контексте, по одному коммиту на задачу и по одной строке отчёта; ревью Фазы 6 делается на месте по всем трём осям; слепая приёмка G4 и независимая сверка G2 честно помечаются в отчёте как непроведённые — **они не имитируются собственным перечитыванием**. Проверка, которую нельзя сделать независимо, называется в отчёте пропущенной; подделать её дороже, чем не сделать.

## Когда применять

- Пользователь диктует инициативу и ждёт результат, а не совместную работу над процессом.
- Нужен PRD с декомпозицией на задачи — с исполнением или без.
- «Собери под ключ», «сделай ТЗ и разнеси по задачам», «не задавай лишних вопросов».
- Пользователь хочет, чтобы задачу разобрали с ним вопрос за вопросом, а собрали без него — это **interview**.
- Пользователь хочет утвердить PRD и список задач, но не гонять процесс руками — это **manual**.

**Когда НЕ применять:** правка в одном файле (просто сделай её); совместное написание кода строка за строкой (работай напрямую); инициатива, которая больше одного проекта и чья цель ещё не определена (сначала цель).

## Маршрут

| Фаза | full | semi (по умолчанию) | interview | manual |
|---|---|---|---|---|
| 0 Подготовка | авто | авто | авто | авто |
| 1 Требования | авто | авто | авто | авто |
| 2 Брифинг | пропущен → самобрифинг | только то, что запрос оставил открытым — иногда ничего | состязательный проход, потом каждая развилка | то же |
| 3 PRD и спеки | авто | авто | авто | показать → ждать явного «ок» |
| 4 План задач | авто, только уведомление | авто, можно остановить словом | авто, можно остановить словом | обсудить → ждать явного «ок» |
| 5 Разработка | авто | авто | авто | авто |
| 6 Код-ревью | авто | авто | авто | авто |
| 8 Приёмка | отчёт + «решения за тебя» | отчёт | отчёт | отчёт |

**`polish` добавляет шаг внутрь Фазы 8, в любом режиме** — круги доводки между слепой приёмкой и отчётом. Ни одной клетки выше он не меняет: он ничего не спрашивает и ничего не согласовывает.

**`interview` и `manual` различаются ровно двумя клетками** — гейт PRD и гейт плана. Если начинаешь различать их где-то ещё, одна из двух строк неверна.

**Гейты манифеста работают в любом режиме.** Это проверки против собственных слов пользователя, а не запросы его времени: ни один режим не покупает право их пропустить.

| Гейт | После фазы | Условие прохода |
|---|---|---|
| **G1** | 2 Брифинг | у каждого требования есть статус; ни одного `open` без записанной причины |
| **G2** | 3 PRD и спеки | каждое живое требование `in-spec`, `deferred` или `dropped`, ноль `open` — **и независимый читатель, получивший только запрос и PRD со спек-дельтами, не находит потерь** |
| **G3** | 4 План задач | каждое `in-spec` ложится хотя бы в одну задачу, **и каждая задача возводится хотя бы к одному требованию** |
| **G4** | 8 Приёмка | слепая приёмка против запроса, PRD не выдаётся; каждое расхождение с манифестом — в отчёт |

**G2 и G4 — одна и та же проверка на двух концах маршрута, и нужны обе.** Обе меряют результат словами пользователя, отняв твой пересказ: G2 — пока ответ стоит абзаца PRD, G4 — когда это последний шанс узнать. Всё между ними меряется спекой, потому что именно её получили исполнители; судить субагента словами, которых он не видел, — производить находки, с которыми никто ничего не может сделать.

Провал гейта — не предупреждение. Фаза переделывается — см. `phases/1-manifest.md`.

**План можно править; запрос — нет.** Когда сборка доказывает, что план неверен — модель данных не держит, предполагаемый контракт бэка не существует, — спека правится, а строка `D##` записывает, что именно показал код и на какой задаче. Это единственное, что попадает в манифест после брифинга; требование оно не снимает никогда и путём для хорошей идеи не служит. Правила — `phases/5-repair.md`.

## Хранилище

Где живут документы, решается в Фазе 0 **обнаружением, а не вопросом** — так же, как выбирается файл памяти проекта:

| Найдено | Режим | `prdRoot` | Живые спеки |
|---|---|---|---|
| клон с remote `thothlab/openclaw_obsidian_vault` | **vault** | `<vault>/Projects/<project>/<initiative>/PRD/` | `<vault>/.../<initiative>/specs/` |
| vault не найден | **local** | `<repo>/.prd/` | `<repo>/.prd/specs/` |

Имена файлов внутри `prdRoot` **одинаковые в обоих режимах** — так ни одна фаза не должна помнить, где она работает. Всё, что специфично для vault (OKF-frontmatter, `index.md`, `Правки.md`, `[[wikilinks]]`, коммит в репозиторий vault), в local-режиме просто не выполняется, и об этом говорится одной строкой во вступительном блоке. Подробности — `phases/0-preflight.md`.

```
<prdRoot>/
├── prd_XX_brief.md          слова пользователя, дословно, после редактирования секретов — не редактируется
├── prd_XX_manifest.md       R01…Rnn — требования и их судьба
├── prd_XX_scratch.md        находки сбора контекста и «осталось неизвестным»
├── prd_XX_reference.md      эталон — только то, что назвал пользователь
├── prd_XX_<slug>.md         сам PRD
├── prd_XX_interfaces.md     границы из спеки, потом — что построили готовые задачи
├── prd_XX_task_NN_<slug>.md задачи
├── prd_XX_rep_NN_<slug>.md  отчёты по задачам
├── prd_XX_handoff_NN_M.md   только когда задача переросла один контекст
├── specs/<domain>/spec.md   спек-дельты этого PRD: ADDED / MODIFIED / REMOVED
├── analysis/<name>.md       длинные разборы: карты, дампы, находки
└── run/prd_XX/
    ├── state.js             состояние прогона — единственный файл, который ты пишешь
    ├── dashboard.html       человеческий вид — копируется один раз и больше не трогается
    └── README.md            как читать эту папку — для человека, пишется один раз

CLAUDE.md | AGENTS.md        память проекта — то, что следующая сессия прочитает первым
```

Живые спеки (`<livingSpecs>/<domain>/spec.md`) — источник правды за пределами одного PRD. Новый PRD пишется **дельтой** к ним, а не с нуля; вливаются дельты только на архивировании (`phases/10-archive.md`).

**Пакет коммитится, а не игнорируется** — это запись пользователя о том, что было обещано и что сделано. Прогон, не оставивший ничего в `prdRoot`, не состоялся.

## Секреты

Ключи — собственность пользователя, а не материал агента. Этот раздел связывает все фазы; фазы его не повторяют.

- **Никогда не спрашивать.** Ни ключ, ни токен, ни пароль, ни строка подключения, ни номер карты не бывают вопросом. *Какой* провайдер — вопрос. *Есть ли* аккаунт — вопрос. Ключ — нет.
- **Редактировать на входе, до любой записи.** Запрос, каждый ответ пользователя и каждый вставленный фрагмент проходят гейт редактирования из `phases/1-manifest.md` *до* того, как попадут в файл. Найденный секрет становится `[REDACTED:<VAR_NAME>]` — имя переменной выживает, значение нет.
- **«Дословно» всегда значит «дословно после редактирования».** Везде, где скилл просит точные слова пользователя, он просит их отредактированными. Это одно правило, а не два.
- **Ссылаться по имени.** `STRIPE_SECRET_KEY`, а не значение. Значение пользователь вписывает в `.env` сам; `.env` лежит в `.gitignore` до первого коммита; в отчёте перечислены имена, которые остались пустыми.
- **Утёкший секрет — условие остановки.** Секрет, попавший в файл или в коммит, называется немедленно, простым языком, с советом отозвать. Перед первым коммитом гейт редактирования прогоняется по всему `prdRoot`.

## Коммиты

Язык сообщения, длина, символы, атомарность, стейджинг и политика пуша берутся из глобального CLAUDE.md пользователя, если он есть, — это единственный источник. Своего у скилла два:

- **Префикс:** `prd-XX: <что сделано>`, со ссылкой на раздел ТЗ (`§N.N`) в скобках, когда коммит закрывает конкретное требование.
- **Маршрутизация:** документы PRD → репозиторий vault (в vault-режиме; пушится в Фазах 7 и 10); код → кодовый репозиторий, **никогда не пушится автоматически**.

Один коммит на задачу — это точки отката пользователя. Правила коммитов говорят, *как* делить и стейджить; они не возвращают шаг «спросить разрешение».

## Суждение

Скилл описывает процесс, а не продукт. Его числа — ярусы, количество вопросов, число историй, ширина волны — **калибровка для первой прикидки, а не цели, которых надо достичь.** PRD, написанный ради числа историй, или план, нарезанный, чтобы попасть в ярус, оптимизирован под правило вместо человека, который попросил.

Правила ниже — того же рода. Каждое здесь потому, что за него заплатили, и каждое — аргумент; аргументы могут проигрывать. Там, где следование правилу сделает результат хуже для пользователя, нарушь его сознательно, скажи об этом одной строкой и иди дальше. Это решение, а решения записываются. Недопустимо только нарушить молча — или сохранить правило потому, что оно записано.

**Пять правил не калибровка и не проигрывают.** Они держат в любом режиме, на любой глубине, на любом ярусе:

1. **Требование снимает только пользователь**, своими словами, процитированными в манифест.
2. **Секрет никогда не запрашивается, не повторяется и не пишется** — ни в файл, ни в промпт, ни в коммит, ни в отчёт.
3. **Факт не выдумывается.** Сущность, эндпоинт, поле, риск, цена, текст — либо выведены из находки с названным источником, либо это видимая заглушка. Формулировка из общих знаний читается как решение, но решением не является: её никто не принимал.
4. **Необратимое и направленное наружу — вопрос**: пуш, деплой, релиз, оплата, письмо третьей стороне, удаление данных, переписывание истории.
5. **Оркестратор не пишет код проекта.** Его клавиатура достаёт до `prdRoot`, файла памяти проекта и git. Всё остальное — правка на две строки, красный тест, находка ревью — уезжает вниз, субагенту. Правила — `phases/5-subagents.md`.

Всё остальное — аргумент.

## Когда пошло не так

Два каталога лежат в `phases/rationalizations.md` — оправдания, которые заканчиваются тем, что пользователь получает не то, что просил, и красные флаги, означающие, что фазу надо начать заново. **Читай этот файл в трёх случаях: провалился гейт, ты ловишь себя на сборке аргумента в пользу пропуска чего-нибудь, и один раз перед итоговым отчётом.**

Это проверки, а не инструкции: ничто в них не говорит, как вести фазу, и механика каждой фазы проверяется в её собственном файле. Держать пятнадцать тысяч знаков сценариев отказа резидентно с первого хода — в единственном контексте, который никогда не обновляется, — значит платить за них на каждом шаге ради ответов на вопросы, которые приходят на трёх. Пять правил выше должны быть в голове без обращения к файлу; остальное — каталог, а каталог открывают.
